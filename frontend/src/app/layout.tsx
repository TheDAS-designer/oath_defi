import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import Navbar from '@/components/Navbar';
import Footer from '@/components/Footer';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'OATH - Decentralized Credit Protocol',
  description: 'Transform your promises into verifiable on-chain commitments backed by economic collateral on Aptos.',
  keywords: ['DeFi', 'Aptos', 'Credit', 'Protocol', 'Vault', 'Lending'],
  authors: [{ name: 'OATH Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} min-h-screen flex flex-col`}>
        <Navbar />
        <main className="flex-grow">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
} 